classDiagram
    Archive <|-- Artist
    Archive <|-- Collective
    Archive <|-- Institution
    Archive : +int ArchiveNumber
    Archive : +String ArchiveName
    Archive : +String OwnerName
    Archive : +int dateFormat Datecreated
    Archive : +int dateFormat Datelastupdated

    Archive : +int TotalnumberofItems
    Archive: +create()
    Archive: +add()
    Archive: +ispublic()
    class Artist{
      +String ArtistName
      +create()
      +add()
      +delete()
      +update()
    }
    class Collective{

        +String ArtistName
      +create()
      +add()
      +delete()
      +update()
    }
    class Institution{
       +String ArtistName
     +create()
      +add()
      +delete()
      +update()
      +makePublic()
    }
            
